import React from 'react'

export default function App() {
  return (
    <div style={{padding: 24}}>
      <h1>VideoMind AI — Frontend (Skeleton)</h1>
      <p>Frontend is initialized with Vite + React. Run `npm install` then `npm run dev`.</p>
    </div>
  )
}
